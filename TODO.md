# TODO - Make circle avatar clickable and go to profile page

- [x] 1. Create `lib/pages/profile.dart` with a `ProfilePage` widget
- [x] 2. Edit `lib/pages/landing.dart` to make the CircleAvatar clickable and navigate to ProfilePage
- [ ] 3. Run `flutter analyze` to verify no errors
